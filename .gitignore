node_modules/
runs/
.env
.env.*
!.env.example
.local/

# Local credentials and private keys must never enter source control.
auth.json
credentials.json
*.pem
*.key
.npmrc

# Compiled workspaces and portable release outputs.
dist/
.build/

# Shell-loaded local credentials must remain external.
.envrc
