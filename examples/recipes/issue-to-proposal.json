{
  "schemaVersion": 1,
  "id": "issue-to-proposal",
  "title": "Issue to change proposal",
  "description": "Build an investigation packet, then draft a read-only change proposal from it.",
  "steps": [
    { "id": "packet", "capability": "investigation.packet", "input": { "repository": { "$param": "repository" }, "issue": { "$param": "issue" } } },
    { "id": "proposal", "capability": "change.proposal", "input": { "packetJobId": { "$job": "packet" } } }
  ],
  "layout": { "packet": { "x": 80, "y": 80 }, "proposal": { "x": 420, "y": 80 } }
}
